
const theFilter = (numbers) => {
    const names = ['Anna', 'Laura', 'Josh', 'Min', 'Karla'];
    function filterLongNumbers(number)
  {
    if (number.length > 10)
    {
      return false
    }
    else {
      return true
    }
  }
}
module.exports = theFilter;
module.exports = filterLongNumbers;

